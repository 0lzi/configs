version: '3'
services:
  homeassistant:
    container_name: home-assistant
    image: omeassistant/raspberrypi3-homeassistant:stable
    volumes:
      - ./config:/config
    environment:
      - TZ=Europe/London
    restart: always
    network_mode: host